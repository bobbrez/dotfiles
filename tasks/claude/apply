#!/usr/bin/env bash
#MISE description="Install Claude-related apps and VS Code extension"
#MISE depends=["brew:apply", "vscode:apply"]
#MISE sources=["tasks/claude/Brewfile"]
set -euo pipefail

source "$MISE_PROJECT_ROOT/lib/brew.sh"
ensure_brew "$MISE_PROJECT_ROOT/tasks/claude/Brewfile"
