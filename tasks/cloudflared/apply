#!/usr/bin/env bash
#MISE description="Install cloudflared CLI (further configuration TBD)"
#MISE depends=["brew:apply"]
#MISE sources=["tasks/cloudflared/Brewfile"]
set -euo pipefail

source "$MISE_PROJECT_ROOT/lib/brew.sh"
ensure_brew "$MISE_PROJECT_ROOT/tasks/cloudflared/Brewfile"
