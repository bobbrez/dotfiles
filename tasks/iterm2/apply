#!/usr/bin/env bash
#MISE description="Install iTerm2 via Brewfile (preferences wiring currently disabled)"
#MISE depends=["brew:apply"]
#MISE sources=["tasks/iterm2/Brewfile"]
set -euo pipefail

source "$MISE_PROJECT_ROOT/lib/brew.sh"
ensure_brew "$MISE_PROJECT_ROOT/tasks/iterm2/Brewfile"

# Specify the preferences directory
# defaults write com.googlecode.iterm2.plist PrefsCustomFolder -string "/Users/bob/Library/Mobile Documents/com~apple~CloudDocs/iTerm2"

# Tell iTerm2 to use the custom preferences in the directory
# defaults write com.googlecode.iterm2.plist LoadPrefsFromCustomFolder -bool true
